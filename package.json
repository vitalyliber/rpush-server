{
  "name": "rpush-server",
  "engines": {
    "node": "12.19.0"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@prettier/plugin-ruby": "^0.20.1",
    "prettier": "^2.2.0"
  },
  "scripts": {
    "s": "overmind start -f Procfile_dev",
    "build": "cd front && yarn install && yarn build",
    "d": "git push dokku@casply.com:rpush-server master"
  }
}
